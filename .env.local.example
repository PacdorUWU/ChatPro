# Copy this file to .env.local and fill in your values
# This file is for LOCAL DEVELOPMENT ONLY - never commit .env.local to git

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=your-secret-key-here-change-for-production
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
# For local MySQL development:
DATABASE_URL="mysql://chatpro_user:chatpro_password@127.0.0.1:3306/chatpro?serverVersion=8&charset=utf8mb4"

# Or for SQLite (simpler for local development):
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"
###< doctrine/doctrine-bundle ###

# PRODUCTION (Railway) environment variables - Set these in Railway dashboard, NOT in .env.local:
# APP_ENV=prod
# APP_SECRET=<generate-secure-random-value>
# DATABASE_URL=<your-railway-database-connection-string>
